<% if current_admin %>
  <div class="container">
    <div class="row">
      <div class="col-10">
        <h4 class="ml-5 mb-5">店情報新規登録</h4>
        <div class="d-flex">
        <div class="col-2"></div>
        <%= form_with model: ShopInfo.new, url: admin_shop_infos_path do |f| %>
          <table class="col-12">
            <tr>
              <th><%= f.label "店名" %></th>
              <td><%= f.text_field :name, class:"form-control" %></td>
            </tr>
            <tr>
              <th><%= f.label "住所" %></th>
              <td><%= f.text_area :shop_address, class:"form-control" %></td>
            </tr>
            <tr>
              <th><%= f.label "URL" %></th>
              <td><%= f.text_field :shop_url, class:"form_control" %></td>
            </tr>
          </table>
          <p class="text-center"><%= f.submit "新規登録", class:"btn btn-success col-3" %></p>
        <% end %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <h2>このページは管理者のみが閲覧できます。</h2>
  <div class="ml-4 mt-4">
    <p class="ml-5">
      <%= link_to "こちら", admin_session_path, class:"border-bottom border-primary" %> からログインしてください。
    </p>
  </div>
<% end %>