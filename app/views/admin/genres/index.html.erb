<% if current_admin %>
  <h4 class="ml-5 mb-5">ジャンル一覧・追加</h4>
  <div class="container">
    <div class="row">
      <div class="col-8">
        <%= form_with model: @genre, url: admin_genres_path do |f| %>
        <table class="mb-5">
          <tr>
            <th><%= f.label "ジャンル名", class:"mr-5" %></th>
            <td><%= f.text_field :name, class:"col-12" %></td>
            <td><%= f.submit "新規登録", class:"btn btn-success ml-5" %></td>
          </tr>
        </table>
        <% end %>
        <div class="d-flex flex-wrap">
        <% @genres.each do |genres| %>
          <div class="ml-1 mr-5 mb-2">
            <%= genres.name %>
            <%= link_to "編集する", edit_admin_genre_path(genres.id), class:"btn btn-success" %>
          </div>
        <% end %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <h2>このページは管理者のみが閲覧できます。</h2>
  <div class="ml-4 mt-4">
    <p class="ml-5">
      <%= link_to "こちら", admin_session_path, class:"border-bottom border-primary" %> からログインしてください。
    </p>
  </div>
<% end %>
