<% if !@post.exists? %>
 <h2>食べたいリストはありません。</h2></br>
 <%= link_to '戻る', :back, class:"btn btn-info mt-4" %>
<% else %>
<h4>食べたいリスト</h4>
  <div class="container">
    <div class="row">
      <div class="col-8 mx-auto" style="width: 200px;">
        <%= render 'layouts/new' %>
        <%= render 'public/posts/post-index', posts: @post %>
        <%= link_to '戻る', :back, class:"btn btn-info mt-4" %>
      </div>
    </div>
  </div>
<% end %>