<!--退会確認画面-->
<div class="container text-center">
  <!--ゲストに退会させない-->
  <% if @customer.email == 'guest@example.com' %>
    <p>
      ゲストは退会はできません。</br>
      ログアウトボタンを押してログアウトしてください。
    </p>
  <% else %>

    <h4>本当に退会しますか？</h4>
    <p>退会すると、会員登録情報や</br>
      これまでのレビューが閲覧できなくなります。</br>
      退会する場合は、「退会する」をクリックしてください。
    </p>

    <div class="p-3">
      <p>
        <%= link_to "退会しない", my_page_customers_path, class:"btn btn-primary btn-sm mr-3" %>
        <%= link_to "退会する", withdraw_customers_path, method: :patch,"data-confirm" => "本当に退会しますか？", class:"btn btn-danger btn-sm" %>
      </p>
    </div>
  <% end %>
</div>